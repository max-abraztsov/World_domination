const Endpoints = {
    AUTH:{
        LOGIN: "http://127.0.0.1:8000/login_page",
        REFRESH: "/refresh",
        LOGOUT: "/logout",
        PROFILE: "/profile",
    }
}

export default Endpoints;